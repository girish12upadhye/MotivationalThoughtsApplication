server:
  port: 9091
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    password: root
    url: jdbc:mysql://localhost:3306/Motivational_Thoughts
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true
  mail:
    host: smtp.gmail.com
    password:XXXXXXX
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    username: XXXXXXX
    
app:
  messages:
    invalidLogin: Invalid Credentialsssss
    invalidPwds: Both pwds should be same
    pwdUpdatedFailed: Pwd update failed
    regSuccess: Registration Successful
    regFailed: Registration Failed
    regMailSubject: Your Account Created - GirishTechUniverse
    quoteUrl: https://type.fit/api/quotes
